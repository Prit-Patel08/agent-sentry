## flowforge run

Run a command with supervision

### Synopsis

Starts a subprocess, captures its stdout/stderr, and monitors its CPU usage.
Example:
  flowforge run --model gpt-4 -- python3 script.py
  flowforge run --max-cpu 80.0 -- ./my-binary
  flowforge run --no-kill -- python3 stuck.py   (watchdog mode)
  flowforge run --inject-feedback agent_feedback.txt -- python3 agent.py

```
flowforge run -- <command> [args...] [flags]
```

### Options

```
      --deep                        Enable Deep Watch (syscall monitoring)
  -h, --help                        help for run
      --inject-feedback string      Path to feedback file to inject into subprocess stdin
      --max-cpu float               Maximum CPU usage threshold (Default: 60.0) (default 60)
      --model string                Model name for ROI calculation (default "gpt-4")
      --no-kill                     Watchdog mode: log & alert on loops but don't kill the process
      --policy-canary-percent int   Policy canary enforcement percentage (0-100). In canary mode, unsampled runs are log-only (default -1)
      --policy-rollout string       Policy rollout mode: shadow, canary, enforce (default: enforce; shadow-mode remains backward-compatible)
      --shadow-mode                 Policy dry-run mode: evaluate actions but log-only for intervention
```

### Options inherited from parent commands

```
      --config string    config file (default is ./flowforge.yaml)
      --profile string   monitoring profile: light, standard, heavy (overrides config file)
  -v, --verbose          verbose output
```

### SEE ALSO

* [flowforge](flowforge.md)	 - A CLI supervisor for AI agents

###### Auto generated by spf13/cobra on 21-Feb-2026
