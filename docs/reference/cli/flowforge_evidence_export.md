## flowforge evidence export

Export a signed evidence bundle

### Synopsis

Exports evidence JSON artifacts and writes manifest + HMAC signature.

Signing key resolution order:
1. --key
2. FLOWFORGE_EVIDENCE_SIGNING_KEY
3. FLOWFORGE_MASTER_KEY

```
flowforge evidence export [flags]
```

### Options

```
      --audit-limit int      Audit event export limit (default 500)
      --chain-limit int      Incident chain export limit (default 500)
      --decision-limit int   Decision trace export limit (default 500)
  -h, --help                 help for export
      --incident-id string   Optional incident ID for incident_chain.json export
      --key string           Signing key override (supports plain, hex:<key>, base64:<key>)
      --out-dir string       Output directory for evidence bundle (default pilot_artifacts/evidence-<timestamp>)
      --timeline-limit int   Timeline event export limit (default 500)
```

### Options inherited from parent commands

```
      --config string    config file (default is ./flowforge.yaml)
      --profile string   monitoring profile: light, standard, heavy (overrides config file)
  -v, --verbose          verbose output
```

### SEE ALSO

* [flowforge evidence](flowforge_evidence.md)	 - Manage signed evidence bundles

###### Auto generated by spf13/cobra on 22-Feb-2026
