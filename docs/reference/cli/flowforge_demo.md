## flowforge demo

Run a 60-second product demo with automatic runaway recovery

### Synopsis

Runs a deterministic demonstration using the same supervision pipeline as 'run':
1) launches a runaway process under flowforge run,
2) detects runaway behavior,
3) terminates it automatically,
4) restarts a healthy worker,
5) prints an outcome summary.

```
flowforge demo [flags]
```

### Options

```
  -h, --help            help for demo
      --max-cpu float   CPU threshold used to trigger runaway handling (default 30)
```

### Options inherited from parent commands

```
      --config string    config file (default is ./flowforge.yaml)
      --profile string   monitoring profile: light, standard, heavy (overrides config file)
  -v, --verbose          verbose output
```

### SEE ALSO

* [flowforge](flowforge.md)	 - A CLI supervisor for AI agents

###### Auto generated by spf13/cobra on 22-Feb-2026
