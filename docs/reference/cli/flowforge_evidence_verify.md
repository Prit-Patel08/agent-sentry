## flowforge evidence verify

Verify a signed evidence bundle

### Synopsis

Verifies:
1. manifest hash integrity
2. signature validity (HMAC-SHA256)
3. file hash and size for all manifest entries

```
flowforge evidence verify [flags]
```

### Options

```
      --bundle-dir string   Evidence bundle directory to verify
  -h, --help                help for verify
      --key string          Signing key override (supports plain, hex:<key>, base64:<key>)
```

### Options inherited from parent commands

```
      --config string    config file (default is ./flowforge.yaml)
      --profile string   monitoring profile: light, standard, heavy (overrides config file)
  -v, --verbose          verbose output
```

### SEE ALSO

* [flowforge evidence](flowforge_evidence.md)	 - Manage signed evidence bundles

###### Auto generated by spf13/cobra on 22-Feb-2026
